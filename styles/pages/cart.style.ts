import styled from '@emotion/styled';

import { ProdctsSection, ProductsPageTitle } from './product.style';

export const CartSection = styled(ProdctsSection)``;

export const CartPageTitle = styled(ProductsPageTitle)``;

export const AmountSection = styled.section`
  margin: 4em auto 0 auto;
  max-width: 900px;
`;
